<section class="inter-banner">
  <h2>Explore all books</h2>
</section>

<div class="container container-all-books">

  <section>
     <% if @books.empty? %>
       <h3>No Books Entered</h3>
     <% end %>

   <% @books.each do |book| %>
      <div class="col-md-4 books-catalog-iteration">
        <p class='book-image'>
          <%= image_tag(book.book_cover_url, class: 'img-responsive img-ft') if book.book_cover? %>
        </p>
        <p>
          <%= link_to book.title, catalog_path(book), class: 'newest-book-title' %><br>
          <p class='newest-price'><%= number_to_currency(book.price) %></p>
        </p>

      </div>
   <% end %>
   <br>

   <div class="row paginate-catalog">
     <div id="paginate">
       <%= will_paginate @books, render: BootstrapPagination::Rails%>
     </div>
   </div>
  </section>
</div>
