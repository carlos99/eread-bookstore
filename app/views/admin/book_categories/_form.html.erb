<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3 book-form-container">
      <%= form_for([:admin, @book_category], :html => {class: 'new-general-form'}) do |f| %>

        <% if @book_category.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@book_category.errors.count, "error") %> prohibited this book category from being saved:</h2>

            <ul>
            <% @book_category.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group">
            <%= f.text_field :name, class: 'form-control', hide_label: true, placeholder: 'Book Category' %>
        </div>

        <div class="form-group">
          <%= f.submit class: 'btn btn-success'%>
        </div>
      <% end %>
    </div>
  </div>
</div>
